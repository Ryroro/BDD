#include <iostream>
#include <vector>
#include <string>
#include "Encoder.h"

using namespace std;

Encoder::Encoder() : cur_id(2) {}

Encoder::encode(vector<string>& tokens, vector<int>& encoded_literals) {

  cur_id = 2;
  
  auto begin = tokens.begin();
  auto end = tokens.end();

  

}
  
  
